<nav class="navbar navbar-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to image_tag('logo.png', width: '40', height: '40'), root_path, {:class=>"navbar-brand" } %>
      <%= link_to 'ELECTRE', root_path, { :class=>"navbar-brand" } %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% if current_user %>
          <li class="<%= 'active' if current_page? (projects_path) %>"><a href="<%= projects_path %>"><%= 'My projects' %></a></li>
          <li class="<%= 'active' if current_page? (employees_path) %>"><a href="<%= employees_path %>"><%= 'All employees' %></a></li>
          <li class="<%= 'active' if current_page? (users_path) %>"><a href="<%= users_path %>"><%= 'All users' %></a></li>
          <li class="<%= 'active' if current_page? (criterions_path) %>"><a href="<%= criterions_path %>"><%= 'All criteria' %></a></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="<%= 'active' if current_page? (edit_user_registration_path) %>"><a href="<%=edit_user_registration_path%>"><span class="glyphicon glyphicon-user"></span> <%=current_user.email%></a></li>
            <li>
              <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="glyphicon glyphicon-log-out"></i> Logout
              <% end %>
            </li>
        <% else %>
            <li><a href="<%=new_user_registration_path%>"><span class="glyphicon glyphicon-user"></span> Register</a></li>
            <li><a href="<%=new_user_session_path%>"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>